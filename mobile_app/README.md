# mobile_app

Flutter mobile application for the "Стильний зубець" grooming salon.

Stack: Flutter, Dart, Supabase, Monobank API.

## Development

```bash
flutter pub get
flutter run
```

For web preview via Docker Compose from the repo root:

```bash
docker compose up mobile-web
```

Web preview: http://localhost:8080
