# web-app

Web application and CRM for the "Стильний зубець" grooming salon.

Stack: React 19, TypeScript, Vite, Tailwind CSS, Supabase.

## Development

```bash
npm install
npm run dev
```

Or via Docker Compose from the repo root:

```bash
docker compose up web-app
```

Dev server: http://localhost:3000
